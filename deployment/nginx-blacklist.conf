server {
    listen 80;
    server_name blacklist.jclee.me;

    # HTTP to HTTPS redirect (선택사항)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:2541;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 헬스체크 엔드포인트
    location /health {
        proxy_pass http://localhost:2541/health;
        access_log off;
    }
}

# HTTPS 설정 (선택사항)
# server {
#     listen 443 ssl http2;
#     server_name blacklist.jclee.me;
#
#     ssl_certificate /etc/letsencrypt/live/blacklist.jclee.me/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/blacklist.jclee.me/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:2541;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }