# This is a template. Do not commit actual secrets to Git!
# Use sealed-secrets or external-secrets in production
apiVersion: v1
kind: Secret
metadata:
  name: blacklist-secrets
  labels:
    app: blacklist
type: Opaque
stringData:
  # Authentication credentials
  REGTECH_USERNAME: "nextrade"
  REGTECH_PASSWORD: "CHANGE_ME"  # Replace with actual password
  SECUDIUM_USERNAME: "nextrade"
  SECUDIUM_PASSWORD: "CHANGE_ME"  # Replace with actual password
  
  # Security keys
  SECRET_KEY: "CHANGE_ME_RANDOM_32_CHARS"  # Generate random string
  JWT_SECRET_KEY: "CHANGE_ME_RANDOM_32_CHARS"  # Generate random string
  API_SECRET_KEY: "CHANGE_ME_RANDOM_32_CHARS"  # Generate random string