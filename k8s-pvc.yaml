apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blacklist-data-pvc
  namespace: blacklist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blacklist-logs-pvc
  namespace: blacklist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi