apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blacklist

resources:
- namespace.yaml
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml

commonLabels:
  app: blacklist
  managed-by: kustomize

images:
- name: blacklist
  newName: registry.jclee.me/blacklist
  newTag: 1.0.9
