apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: blacklist-base
  annotations:
    description: "Blacklist 애플리케이션 기본 매니페스트"

resources:
- deployment.yaml
- service.yaml
- configmap.yaml
- secret.yaml

commonLabels:
  app: blacklist
  tier: application
  managed-by: argocd

images:
- name: registry.jclee.me/jclee94/blacklist
  newTag: latest