apiVersion: v1
kind: Secret
metadata:
  name: blacklist-secrets
  labels:
    app: blacklist
    app.kubernetes.io/name: blacklist
type: Opaque
stringData:
  # Application secrets
  SECRET_KEY: "change-in-production-k8s-secret"
  JWT_SECRET_KEY: "change-in-production-jwt-secret"
  
  # External API credentials (should be updated via kubectl or ArgoCD)
  REGTECH_USERNAME: ""
  REGTECH_PASSWORD: ""
  SECUDIUM_USERNAME: ""
  SECUDIUM_PASSWORD: ""