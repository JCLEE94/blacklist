apiVersion: v1
kind: Secret
metadata:
  name: blacklist-secrets
  labels:
    app: blacklist
    component: secrets
type: Opaque
stringData:
  # Application secrets (to be replaced by external secret management)
  SECRET_KEY: "changeme-production-secret"
  JWT_SECRET_KEY: "changeme-jwt-secret"
  
  # External API credentials (to be replaced by external secret management)
  REGTECH_USERNAME: "changeme"
  REGTECH_PASSWORD: "changeme"
  SECUDIUM_USERNAME: "changeme"
  SECUDIUM_PASSWORD: "changeme"