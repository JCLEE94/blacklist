apiVersion: v1
kind: Service
metadata:
  name: blacklist-service
  labels:
    app: blacklist
    component: api
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 32542
    nodePort: 32452
    protocol: TCP
    name: http
  selector:
    app: blacklist
    component: api