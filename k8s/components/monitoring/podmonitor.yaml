apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: blacklist
  labels:
    app: blacklist
spec:
  selector:
    matchLabels:
      app: blacklist
  podMetricsEndpoints:
  - port: http
    interval: 30s
    path: /metrics