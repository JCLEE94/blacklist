apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blacklist-new

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - cronjob.yaml
  # ArgoCD 관련 파일은 별도로 적용
  # - argocd-app.yaml
  # - argocd-image-updater-config.yaml

images:
  - name: registry.jclee.me/blacklist
    newTag: latest