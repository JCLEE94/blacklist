---
apiVersion: v1
kind: Secret
metadata:
  name: blacklist-secrets
  namespace: blacklist
  labels:
    app.kubernetes.io/name: blacklist
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # NOTE: Replace these with base64 encoded values in production
  # Use: echo -n 'your-value' | base64
  REGTECH_USERNAME: "changeme"
  REGTECH_PASSWORD: "changeme"
  SECUDIUM_USERNAME: "changeme"
  SECUDIUM_PASSWORD: "changeme"
  SECRET_KEY: "changeme"
  JWT_SECRET_KEY: "changeme"
  DEFAULT_API_KEY: "blk_changeme"
  ADMIN_PASSWORD: "changeme"