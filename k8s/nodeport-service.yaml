apiVersion: v1
kind: Service
metadata:
  name: blacklist-nodeport
  namespace: blacklist
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: blacklist
    app.kubernetes.io/name: blacklist
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8541
    nodePort: 32542