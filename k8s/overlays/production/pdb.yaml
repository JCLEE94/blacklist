apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: blacklist-pdb
  namespace: blacklist-prod
spec:
  minAvailable: 2  # 최소 2개의 Pod는 항상 실행
  selector:
    matchLabels:
      app: blacklist
      environment: production
  maxUnavailable: 1  # 동시에 1개까지만 중단 가능