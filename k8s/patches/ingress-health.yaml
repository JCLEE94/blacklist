apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blacklist
  namespace: blacklist
  annotations:
    # Force ArgoCD to consider Ingress as healthy
    argocd.argoproj.io/hook: Skip
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true