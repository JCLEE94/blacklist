apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blacklist-data
  namespace: blacklist
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: blacklist-data-pv
  storageClassName: ""
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blacklist-logs
  namespace: blacklist
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeName: blacklist-logs-pv
  storageClassName: ""
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blacklist-instance
  namespace: blacklist
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: blacklist-instance-pv
  storageClassName: ""