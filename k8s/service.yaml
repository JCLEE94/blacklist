apiVersion: v1
kind: Service
metadata:
  name: blacklist
  namespace: blacklist
  labels:
    app: blacklist
spec:
  type: ClusterIP
  selector:
    app: blacklist
  ports:
  - port: 8541
    targetPort: 8541
    protocol: TCP
    name: http
---
apiVersion: v1
kind: Service
metadata:
  name: blacklist-nodeport
  namespace: blacklist
  labels:
    app: blacklist
spec:
  type: NodePort
  selector:
    app: blacklist
  ports:
  - port: 8541
    targetPort: 8541
    nodePort: 32452
    protocol: TCP
    name: http