apiVersion: v1
kind: Service
metadata:
  name: blacklist-nodeport
  namespace: blacklist
  labels:
    app: blacklist
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 2541
    nodePort: 32542
    protocol: TCP
    name: http
  selector:
    app: blacklist