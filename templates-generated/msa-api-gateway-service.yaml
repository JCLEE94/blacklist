apiVersion: v1
kind: Service
metadata:
  labels:
    app: blacklist-api-gateway
    generated-by: template-generator
    service: api-gateway
  name: blacklist-api-gateway
  namespace: blacklist
spec:
  ports:
  - name: http
    nodePort: 30806
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: blacklist-api-gateway
  type: NodePort
